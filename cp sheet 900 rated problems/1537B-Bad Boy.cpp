#define ll long long
#include <bits/stdc++.h>
using namespace std;


void solve(){
    int n,m,i,j;
    cin >> n >> m >> i >> j;
    
    cout << 1 <<" " << 1 << " " << n << " " << m << endl;
    //keeping both the yo-yo's at the opposite diagonal end to maximise the distance
}

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    ll t;
    cin >> t;
    while(t--){
        solve();
    }
    return 0;
}